# 线性回归学习笔记
> 整理笔记

## 1.回归分析基础
回归分析：根据数据，确定**两种或两种以上变量之间相互依赖的定量关系**。

通用函数表达式：
$$y = f(x_1,x_2 \dots x_n)$$

### 回归分类
1. **按照变量数量划分**
- 一元回归：$y=f(x)$，只有1个自变量
- 多元回归：$y=f(x_1,x_2\dots x_n)$，2个及以上自变量

2. **按照函数关系划分**
- 线性回归：$y=ax+b$
- 非线性回归：例如 $y=ax^2+bx+c$

> ❗ 易错点：不是看画出来是直线还是曲线，**看对待求参数是否为一次关系**。

## 2.什么是线性回归（重点补充）
> 线性回归：因变量$y$和**模型参数（系数、截距）呈线性关系**，**不是必须和自变量x是直线关系**。

✅属于线性回归（参数全部是一次）
1. $y=ax+b$ ——一元，图像直线
2. $y = a\cdot t^2 + S_0$ ——把 $t^2$ 当作新特征$z$，改写 $y= a\cdot z+S_0$；虽然图像抛物线（曲线），参数$a、S_0$是一次，**仍然属于线性回归（多项式线性回归）**。

❌不属于线性回归（参数出现在指数、分母，函数内部，参数之间相乘，参数高次方）
$$y = S_0 e^{a t}$$
参数$a$在指数，无法通过替换自变量，写成参数的一次组合 → **真正非线性回归，不能直接最小二乘闭式求解**。

> 物理例子
- 匀速运动 $S = v\cdot t + S_0$：线性回归
- 匀加速 $S = a\cdot t^2+S_0$：**依旧属于线性回归**，很多课件这里会写错，误以为是非线性。

### 一元线性回归公式
$$\hat y = ax + b$$
- $a$：斜率（特征系数）
- $b$：截距
- $\hat y$：模型预测输出值


## 3.损失函数：如何寻找最合适的参数a,b
目标：让模型预测输出 $\hat y$ 尽可能接近真实$y$。
最小二乘思想：最小化全部样本预测值与真实值的误差平方总和。

$$\min \left\{ \sum_{i=1}^{m} (\hat y_i-y_i)^2 \right\}$$

$m$代表样本数量。

为了梯度下降求导化简，引入 $\frac1{2m}$，得到损失函数$J$：
$$J=\frac{1}{2m}\sum_{i=1}^m(\hat y_i-y_i)^2=\frac{1}{2m}\sum_{i=1}^m(ax_i+b-y_i)^2=g(a,b)$$

> $\frac12$：求导和平方求导出来的2抵消，简化公式；
> $\frac1m$：取样本平均，损失、梯度大小不受样本总数影响，调学习率更方便。

## 4.梯度下降法求解参数
> 梯度下降：寻找函数极小值的算法；沿着梯度的反方向，按步长迭代搜索，直到收敛。

通用迭代更新公式：
$$p_{i+1}=p_i-\alpha \frac{\partial}{\partial p_i}f(p_i)$$
- $p_i$：当前参数
- $\alpha$：学习率（步长）
- $\frac{\partial f}{\partial p_i}$：梯度（偏导数）

### 针对一元线性回归，a、b必须同步更新！
> ⚠️ 关键点：**a、b必须使用旧参数同时计算梯度，算出临时变量，最后一次性赋值更新，不能更新完a，拿新a算b**。

$$
\begin{cases}
temp_a = a-\alpha \frac{\partial}{\partial a}g(a,b)= a-\alpha \frac1m\sum_{i=1}^m(ax_i+b-y_i)x_i \\
temp_b = b-\alpha \frac{\partial}{\partial b}g(a,b)= b-\alpha \frac1m\sum_{i=1}^m(ax_i+b-y_i)
\end{cases}
$$
$$a=temp_a,\quad b=temp_b$$

循环迭代，当$J$几乎不再下降，判定收敛，停止迭代。

### 为什么参数必须是一次？——从梯度下降看本质
> 核心机制：对"一次"的参数求偏导，参数会从公式里"退下来"；对"非一次"的参数求偏导，参数还会卡在非线性函数里。

#### 1）参数一次：求导后参数退下来
一元线性回归损失函数：
$$J(a,b)=\frac{1}{2m}\sum_{i=1}^m(ax_i+b-y_i)^2$$

对 $a$ 求偏导：
$$\frac{\partial J}{\partial a}=\frac{1}{m}\sum_{i=1}^m(ax_i+b-y_i)\,x_i$$

对 $b$ 求偏导：
$$\frac{\partial J}{\partial b}=\frac{1}{m}\sum_{i=1}^m(ax_i+b-y_i)$$

求导后，偏导变成了 $a、b$ 的**一次函数**，带来两个连锁好处：
1. 损失函数 $J$ 是 $a、b$ 的二次函数 → 图形是"开口向上的碗"（凸函数）→ **只有一个最低点**。
2. 偏导是线性的，令其 $=0$ 得到 $a、b$ 的**线性方程组** → 可直接解出（正规方程）。

👉 梯度下降**从任意起点出发，都一定滑到同一个最低点，永不卡住**。

##### 附：为什么 $J$ 一定是二次函数？——固定一个参数验证

固定 $b$，只把 $a$ 当作变量，展开损失函数：

$$J(a)=\frac1{2m}\sum_{i=1}^m \big(a x_i + b - y_i\big)^2$$

令 $d_i = b - y_i$，这里 $b$ 被固定住，是常数，不是变量：

$$J(a)=\frac1{2m}\sum_{i=1}^m \big(\boldsymbol{a x_i}+d_i\big)^2$$

完全平方展开：

$$(a x_i+d_i)^2 = x_i^2\,a^2 + 2x_i d_i\,a + d_i^2$$

$$
\begin{aligned}
J(a)
&=\frac{1}{2m}\sum_{i=1}^m \Big[ x_i^2\,a^2 + 2x_i d_i\,a + d_i^2 \Big] \\[4pt]
&=\underbrace{\left(\frac{1}{2m}\sum x_i^2\right)}_{A}a^2
+\underbrace{\left(\frac{1}{2m}\sum 2x_i d_i\right)}_{B}a
+\underbrace{\frac{1}{2m}\sum d_i^2}_{C}
\end{aligned}
$$

得到一元二次形式：

$$\boldsymbol{J(a)=A\,a^2+B\,a + C}$$

**$b$ 去哪里了？** $d_i=\boldsymbol{b-y_i}$

- $A=\displaystyle\frac{1}{2m}\sum x_i^2$：$A$ 里面没有 $b$
- $B=\displaystyle\frac{1}{m}\sum x_i(b-y_i)$：$B$ 里面包含 $b$
- $C=\displaystyle\frac{1}{2m}\sum(b-y_i)^2$：$C$ 里面也包含 $b$

> ⚠ 关键前提：我们此时**固定 $b$ 当作常数，只视 $a$ 为函数自变量**。
> 虽然 $B、C$ 的数值大小受 $b$ 影响，但 $B、C$ 对于变量 $a$ 来说，都只是常数系数。
> 所以函数形式依旧是标准一元二次 $Aa^2+Ba+C$。
> 又因 $A=\frac1{2m}\sum x_i^2>0$，开口向上 → **从数学上验证了 $J$ 是凸函数，只有一个最低点**。

#### 2）参数非一次：求导后参数仍被困住
以 $y=ae^{bx}$（参数 $b$ 在指数）为例，损失函数：
$$J(a,b)=\frac{1}{2m}\sum_{i=1}^m(ae^{bx_i}-y_i)^2$$

对 $b$ 求偏导（链式法则）：
$$\frac{\partial J}{\partial b}=\frac{1}{m}\sum_{i=1}^m(ae^{bx_i}-y_i)\cdot ae^{bx_i}\cdot x_i$$

$b$ 仍在 $e^{bx}$ 里，求导后没有退下来，因此：
1. 偏导是 $b$ 的**非线性函数** → 令其 $=0$ 得到的是**超越方程**，无闭式解。
2. 损失函数对 $b$ **非凸** → 有**多个局部最低点**。

👉 梯度下降可能**卡在局部最优**，且结果取决于起点。

> 一句话总结：**参数一次 → 损失函数是凸的二次碗 → 梯度下降必收敛到唯一全局最优；参数非一次 → 损失函数非凸 → 可能卡在局部最优。**

## 5.多元因子线性回归
现实场景：房价不只是受面积影响；还受收入、房龄、房间数量、人口等多个因子共同影响。

### 数学模型
$$\hat y = b_0 + b_1x_1 + b_2x_2 + b_3x_3 + ... +b_nx_n$$
- $b_0$：截距 `intercept_`
- $b_1,b_2...b_n$：每一个特征对应的系数 `coef_`
- $x_1,x_2...x_n$：各个自变量（多元因子）

> 系数含义：其他特征保持不变，该特征每增加1单位，预测$\hat y$平均变化$b_i$。

### 多元线性回归：同样使用损失函数做梯度下降迭代
> ✅重点：**多元线性回归依然使用同样形式的损失函数，只是待求解的参数变多**。

#### 1）多元损失函数
$$
J=\frac{1}{2m}\sum_{i=1}^m\left( \hat y_i - y_i\right)^2
=\frac{1}{2m}\sum_{i=1}^m\Big(b_0 + b_1x_{i1}+b_2x_{i2}+...+b_nx_{in} - y_i\Big)^2
$$
- $x_{i1}$：第$i$个样本的第1个特征；
- 目标：最小化损失$J$。

#### 2）多元梯度下降迭代逻辑
一元只有两个参数$a、b$；多元有：$b_0$(截距)、$b_1、b_2…b_n$（各个特征系数）。

1. 全部参数**先初始化**（一般初始化为0）；
2. 用**当前全部旧参数**，对每一个参数分别求偏导，算出每一个参数的梯度；
    $$
    \frac{\partial J}{\partial b_j}=
    \frac1m\sum_{i=1}^m \Big(\hat y_i-y_i\Big)\cdot x_{ij}
    $$
    > 对于截距$b_0$，相当于$x_{i0}=1$；
3. 全部参数先计算**临时更新值temp**，**不立刻覆盖原参数**；
4. 所有临时值全部算完，**一次性同步更新全部参数**；
5. 不断循环，观察损失$J$；当损失几乎不再下降 → 判定收敛，迭代结束。

> ⚠️核心规则和一元完全一样：**必须同步更新所有参数，不能更新一个参数就立刻用它去算下一个梯度**。

